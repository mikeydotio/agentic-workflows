node_modules/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.swp
*.swo
*~
# .storyhook/ and .planning/ are version-controlled project data — do not ignore

# Freshen plugin signals (ephemeral, consumed on clear)
.freshen/

# RCA investigation artifacts (ephemeral, per-investigation)
.rca/

# Pilot plugin runtime artifacts (gitignored)
.pilot/state.json
.pilot/lock.json
.pilot/verdicts.jsonl
# .pilot/config.json, .pilot/plan-mapping.json, .pilot/handoffs/, and .pilot/fix-cycles/ are version-controlled — do not ignore
